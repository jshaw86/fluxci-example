---
apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  name: charts-repository
  namespace: flux-system
spec:
  interval: 30s
  ref:
    branch: master
  url: https://github.com/jshaw86/fluxci-example-charts
  ignore: |
    # exclude all
    /*
    # include charts directory
    !/deploy/
